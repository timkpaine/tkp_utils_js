dist: xenial
language: node_js
node_js: "12"
cache: npm

before_install:
    - if [ "$TRAVIS_OS_NAME" = "osx" ]; then npm install -g yarn; fi

install:
  - node --version
  - yarn

script:
  - yarn lint
  - yarn build
  - yarn test

branches:
  only:
  - master

notifications:
  email: false
